name: PlatformIOBuild

# env:
#   PROJECT_DIR: examples/Test/build_test

on:
  push:
    paths:
    - '**.ino'
    - '**.cpp'
    - '**.hpp'
    - '**.h'
    - '**.c'
    - '**PlatformioBuild.yml'
  pull_request:
  workflow_dispatch:

jobs:
  build:
    name: ${{ matrix.board }}@${{ matrix.gfx-type }}

    runs-on: ubuntu-latest

    strategy:
      matrix:

        board:
          - m5stack-core

        gfx-type:
          - lgfx
          - m5gfx

        sketch-dir:
          - emojis
          - LVGL_Fonts

        include:
          - { board: m5stack-core, gfx-type: lgfx, ... }
          - { board: m5stack-core, gfx-type: m5gfx, ... }

      fail-fast: false

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: ${{ github.event.pull_request.head.sha }}

      - name: Cache pip
        uses: actions/cache@v5
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-
      - name: Cache PlatformIO
        uses: actions/cache@v5
        with:
          path: ~/.platformio
          key: ${{ runner.os }}-${{ hashFiles('**/lockfiles') }}
      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.13'

      - name: Install PlatformIO
        run: |
          python -m pip install --upgrade pip
          pip install --upgrade platformio

      - name: Run PlatformIO

        run: |
          cd examples/${{ matrix.sketch-dir }}
          export pio_ver=${{ matrix.gfx-type }}
          export pio_env="${pio_ver//./_}"
          pio pkg install -e ${{ matrix.board }}-$pio_env --no-save --library file://$(realpath ../../../)
          pio run -e ${{ matrix.board }}-$pio_env
